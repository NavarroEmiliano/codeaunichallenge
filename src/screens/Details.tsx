import React from 'react';
import { Text, View } from 'react-native';

type Props = {};

const Details = (props: Props) => {
  return (
    <View>
      <Text>Holis, soy details</Text>
    </View>
  );
};

export default Details;
